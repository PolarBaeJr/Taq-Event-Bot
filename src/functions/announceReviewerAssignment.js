/*
  Auto-generated function mirror for easier reading/navigation.
  Source of truth remains in src/index.js.
*/

async function announceReviewerAssignment({
  state,
  application,
  trackKey,
  trackLabel,
  channelId,
  threadId,
}

module.exports = announceReviewerAssignment;
