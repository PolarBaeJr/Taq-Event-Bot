/*
  Auto-generated function mirror for easier reading/navigation.
  Source of truth remains in src/index.js.
*/

function makeApplicationPostContent({
  applicationId,
  trackKey,
  applicantMention,
  applicantRawValue,
  headers,
  row,
}

module.exports = makeApplicationPostContent;
